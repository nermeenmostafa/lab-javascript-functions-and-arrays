//Find the maximum
    if (num1 > num2) {
        return num1;
    } else if (num2 < num1) {
        return num2;
    } else if (num1 === num2) {
        return num1;
    }


// Find the longest word

